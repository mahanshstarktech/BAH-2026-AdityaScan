# AdityScan v4.1 — Production Requirements (SOTA Physics + Conformal Prediction)
# Python 3.11+ required

# ── Core data processing ─────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.0.0
astropy>=6.0.0          # FITS handling, WCS, coordinate transforms
sunpy>=5.0.0            # SolarPy: GOES download, JSOC queries, HEK client
cdflib>=1.2.0           # CDF file reading (ASPEX-SWIS, PAPA)
netCDF4>=1.6.0          # MAG netCDF files
scikit-image>=0.22.0    # Image resizing (SUIT downsampling)
PyWavelets>=1.4.0       # CWT Morlet wavelet for QPP detection (SOTA)
drms>=0.7.0             # NASA JSOC SHARP magnetic parameter downloads

# ── Machine Learning ─────────────────────────────────────────────────────────
torch>=2.2.0            # Neural network training + MC Dropout inference
torchvision>=0.17.0     # EfficientNet-B0 image branch
onnxruntime>=1.17.0     # CPU ONNX inference (2-5x faster than PyTorch)
xgboost>=2.0.0          # Phase C fine-tuning head (small Aditya-L1 dataset)
scikit-learn>=1.4.0     # RobustScaler, calibration, ROC AUC
joblib>=1.3.0           # Model serialization

# ── API framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
websockets>=12.0

# ── Task queue ────────────────────────────────────────────────────────────────
celery[redis]>=5.3.0
redis>=5.0.0

# ── Database ──────────────────────────────────────────────────────────────────
asyncpg>=0.29.0         # Async PostgreSQL (TimescaleDB)
psycopg2-binary>=2.9.0  # Sync PostgreSQL (schema init)
sqlalchemy>=2.0.0       # ORM + connection pool

# ── Storage (Cloudflare R2) ───────────────────────────────────────────────────
boto3>=1.34.0           # S3-compatible (R2 uses same API)

# ── Monitoring ────────────────────────────────────────────────────────────────
prometheus-client>=0.20.0
structlog>=24.0.0

# ── HTTP / networking ─────────────────────────────────────────────────────────
requests>=2.31.0
httpx>=0.26.0           # Async HTTP (for NOAA SWPC, JSOC)
aiohttp>=3.9.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
python-dateutil>=2.8.0
pytz>=2024.1
tqdm>=4.66.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
